# 英文-raw
```
Generate an SQL query based on a prompt. Do not reply with anything besides the SQL query.

The prompt is: {prompt}

The available columns are: {columns}

The table name is: {table_name}
```

# 中文-translate
```
根据提示生成SQL查询。除了SQL查询之外，不要回复任何内容。
提示为：{prompt}

可用列有：｛columns｝
表名为：｛table_name｝
```